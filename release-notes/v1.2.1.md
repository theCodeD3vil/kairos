# Kairos v1.2.1

## VS Code Extension

- Fixed activation failures caused by a corrupted local outbox database.
- Corrupted outbox files are now moved aside with a `.corrupt.*` suffix before a fresh outbox database is initialized.
- Added Kairos output-channel logging for corrupt database recovery.

## Tests

- Added regression coverage for corrupted outbox startup recovery.
