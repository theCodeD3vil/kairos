# AGENTS.md

## Codex Instructions
- Always ask follow up questions to understand my task or idea.
- Repo-wide UI rule: all headers must be title-only.
- Do not add descriptive subtitle/description text directly under headers in any app UI unless explicitly requested by the user.
- For `apps/desktop/frontend`, use JolyUI (`https://jolyui.dev`) as the only UI component source unless the user explicitly asks for another source.
